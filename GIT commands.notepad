{"bgColorIndex":0,"textColorIndex":1,"note":"\n\n\n\"Remote Origin Already Exists\" Error: ==>>>\nhttps:\/\/www.cloudbees.com\/blog\/remote-origin-already-exists-error\n\n\nall remotes have names or aliases\n\ngit remote -v = to list all existing remotes\n\n\nto remove a particular remote\n\n\ngit remote remove <alias>\n\nto rename a particular remote\n\ngit remote rename <old-name> <new-name>\n\n\n\ngit status\n\n\nAdding the URL for the remote repository where your local repository code will be pushed.\n\ngit remote add <alias> <Remote Repo URL>\n\n\nPush the changes in your local repository to GitHub remote repository\n\ngit push <alias> <branch name>\n\n\nTo get all commit ids\n\ngit log\n\nto remove new files from working area\n\ngit clean -f \n\nTo revert to working area from staging area.\n\ngit reset <File Name>\n\n\nto revert the changes committed in a particular commit id from local repo.\n\ngit revert <Commit ID>\n\nto revert the changes from remote repo\n\ngit push origin master -f\n\n\n\nBRANCHES\n\nto list branches\n\ngit branch\n\nto display all the branches in your repo, and also tell you what branch you're currently in.\n\ngit branch -v\n\nto switch branches \n\ngit switch\/checkout <branch name>\n\nto see the difference between two branches\n\ngit diff <branch name> <second branch name>\n\nto merge branches\n\ngit merge <branch name>\n\n\nto Push all branches code to your remote repository\n\ngit push <alias> --all\n\nto Delete a branch in local repo\n\ngit branch -d <branch name>\n\nto delete a remote branch in the remote repo\n\ngit push <alia> --delete <branch name>\n\nTAGS\n\ngit tag : It will displays the tags.\ngit tag <<Tag Name>> : It will create the tag.\ngit push <alias> tag <<Tag Name>> : It will push the tag to remote repo. \n\ngit push <alias>  --tags: It will push all the tags to remote repo.\n\n\nSTASH\n\nto save an ongoing work in a branch\ngit stash  \n\nto list all stashes\n\ngit stash list\/show\ngit stash show -p \n\n\nto resume work on a saved work\n\ngit stash apply <stash ID>\n\n\nto choose a commit from one branch and apply\/merge it onto\nanother.\n\ngit cherry-pick <commit ID>\n\n\nWhat is the difference between git fetch and get pull?\nAns) git fetch : It will get the update from git remote repo and will update your local repo. But it will not merge with Local working copy.\ngit pull : It will get the update from git remote repo and will update your local repo as well it will merge with Local working copy also.\nSo git pull = git fetch + git merge origin\/master\n\n\n\n\nHow to Rename a git branch name?\nAns) git branch -m <oldname> <newname> Or, if you are already in the branch:\ngit branch -m <newname>\n\n\ngit rebase adds each commit, git merge merges all commits into a single new version\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n  "}